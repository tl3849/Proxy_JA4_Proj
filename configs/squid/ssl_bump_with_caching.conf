# Squid configuration for basic HTTP proxy
http_port 3128

# Basic logging
access_log /var/log/squid/access.log
cache_log /var/log/squid/cache.log

# Cache settings
cache_mem 256 MB
maximum_object_size 4096 KB

# Allow all requests
http_access allow all
